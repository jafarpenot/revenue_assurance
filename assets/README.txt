Drop the KPMG logo here as `kpmg_logo.png` (PNG, transparent background
recommended, ~50px tall is enough). The app picks it up automatically at
startup — no code change needed. If the file is missing, the header falls
back to a styled "KPMG" text placeholder.
